<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.studynow.groupus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/final_logo_app"
        android:label="dhobiPress"
        android:roundIcon="@mipmap/final_logo_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".order_done"></activity>
        <activity android:name=".settings" />
        <activity android:name=".help" />
        <activity android:name=".view" />
        <activity android:name=".order" />
        <activity android:name=".complete_registration" />
        <activity android:name=".Dashboard" />
        <activity android:name=".sign_up" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>